version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd web && npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build
        - node generate-manifest.js
        - ls -la .next/
        - cat .next/deploy-manifest.json
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*


